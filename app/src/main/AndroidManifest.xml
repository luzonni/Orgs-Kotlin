<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_zonni"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orgs"
        tools:targetApi="31">
        <activity android:name=".ui.activity.ListProductsActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.LoginActivity"
            android:theme="@style/Theme.Orgs.NoActionBar"/>
        <activity android:name=".ui.activity.RegisterUserActivity"/>
        <activity android:name=".ui.activity.FormProductActivity" />
        <activity android:name=".ui.activity.ProductSpecificationActivity" />
        <activity android:name=".ui.activity.UserPerfilActivity" />
        <activity android:name=".ui.activity.AllProductsActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>